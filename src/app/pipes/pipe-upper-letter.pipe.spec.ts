import { PipeUpperLetterPipe } from './pipe-upper-letter.pipe';

describe('PipeUpperLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeUpperLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
